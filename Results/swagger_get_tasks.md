# Swagger UI - GET /tasks (Exemplo Visual)

Esta imagem mostra a página do Swagger UI após executar o endpoint GET /tasks, exibindo os dados atuais da base.

---

![Swagger UI GET /tasks](swagger_get_tasks.png)

> Para gerar esta imagem, acesse http://localhost:8000/docs, expanda o endpoint GET /tasks e clique em "Execute". O resultado exibirá as tarefas cadastradas no momento.
